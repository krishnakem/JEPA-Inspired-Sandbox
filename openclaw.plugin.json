{
  "id": "jepa-inspired-silicon-sandbox",
  "name": "JEPA-Inspired Silicon Sandbox",
  "description": "Local market-vision simulations powered by a JEPA-inspired Python engine.",
  "version": "0.1.0",
  "kind": "capability",
  "main": "./src/plugin/index.ts",
  "configSchema": {
    "type": "object",
    "properties": {
      "scratchDir": {
        "type": "string",
        "description": "Ephemeral working directory for session state. Leading ~ is expanded at plugin-register time.",
        "default": "~/.jepa-silicon-sandbox/scratch"
      },
      "outputDir": {
        "type": "string",
        "description": "Durable output directory for run artifacts. Leading ~ is expanded at plugin-register time.",
        "default": "~/.jepa-silicon-sandbox/output"
      },
      "agentName": {
        "type": "string",
        "description": "Display name used by Market Vision reports.",
        "default": "JEPA-Inspired Silicon Sandbox"
      },
      "defaultDelayMs": {
        "type": "number",
        "description": "Reserved lifecycle setting kept for compatibility.",
        "default": 750
      }
    },
    "additionalProperties": false
  }
}
